server:
  port: 9191

logging:
  level:
    ex.microservices: debug

spring:
  application:
    name: catalog-service
  cloud:
    config:
      uri: http://localhost:8888/
  zipkin:
    base-url: http://localhost:9411/